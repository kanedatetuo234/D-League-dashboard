# D-League Forge 移行チェックリスト

最終更新：2026-09-13

## 状態の見方

- `[x]` 完了：実際のファイルまたはSupabase画面で確認済み
- `[~]` 作業中：実装途中、または一部だけ完了
- `[ ]` 未着手
- `[?]` 判断待ち：仕様・運用の確認が必要

## 1. 基盤

- `[x]` 新プロジェクト `D-League-Forge` を作成
- `[x]` Supabase Freeプランでプロジェクト作成
- `[x]` Supabase PostgreSQLを作成
- `[x]` Supabase Storageバケット `match-photos` を作成
- `[x]` 公開URLは現行Cloudflare URLを維持する方針を確定
- `[x]` 移行用ローカルリポジトリを作成

## 2. データベース

- `[x]` `members`：メンバー情報
- `[x]` `matches`：対局本体
- `[x]` `match_players`：順位・持ち点・ポイント・席
- `[x]` `schedules`：参加可否
- `[x]` `local_rules`：半荘／東風別ルール
- `[x]` `match_photos`：写真メタ情報
- `[x]` `match_rule_snapshots`：対局登録時のルール保存
- `[x]` `match_change_logs`：登録・修正・削除履歴
- `[x]` 基本的なRLS設定
- `[x]` 既存Supabaseプロジェクトへの追加テーブル反映

## 3. アプリ接続

- `[x]` Supabase接続設定と読み取りアダプターを追加（Supabaseを既定値に変更、写真URLも変換）
- `[x]` `members` 読み込みをSupabaseへ切替
- `[x]` `schedules` 読み込みをSupabaseへ切替
- `[x]` `matches`／`match_players` 読み込みをSupabaseへ切替
- `[x]` `local_rules` 読み込みをSupabaseへ切替
- `[x]` 既存GAS APIとの切替フラグを追加（URLパラメータ`?source=gas`で旧APIへ戻せる）
- `[x]` API失敗時の画面エラー表示を整理（初期読込・各登録ダイアログにエラー表示あり）

## 4. 対局登録・修正

- `[x]` 対局登録・修正・削除Edge Functionを作成・公開（公開Functionで登録・修正・削除を実環境確認。Dashboardでは共有計算ファイルを含めてデプロイ済み）
- `[x]` 1位〜4位へのプレイヤー登録処理
- `[x]` 当日の参加可能メンバーだけを選択肢に表示
- `[x]` 東・南・西・北の席登録処理
- `[x]` 同一日の複数対局に対応するDB構造・登録処理
- `[x]` 順位重複・プレイヤー重複・席重複の入力チェック
- `[x]` 登録時に `match_rule_snapshots` を保存する処理
- `[x]` 登録・修正・削除時に `match_change_logs` を保存する処理

## 5. ポイント計算

- `[x]` 既存GASのポイント計算・入力検証をEdge Function共有モジュールへ移植
- `[x]` 東風ルールの計算をEdge Functionへ移行
- `[x]` 順位点・ウマを反映
- `[x]` 祝儀を反映
- `[x]` 箱下精算を反映
- `[x]` 同点処理を反映
- `[x]` 対局時ルールを使って再計算
- `[x]` 計算結果を `match_players.point` に保存（公開Functionの実環境テストで確認済み）

## 6. 写真

- `[x]` ブラウザ側JPEG圧縮をSupabase版登録処理へ接続
- `[x]` JPEG品質・容量の実測（既存移行写真2ファイルを確認）
- `[x]` Storageへのアップロード（`match-photos` に2ファイル）
- `[x]` `match_photos` へのメタ情報保存（2件）
- `[x]` 写真表示（公開Storage URLへ変換、公開読み取り権限を確認済み）
- `[x]` 写真差し替え・削除（既存写真の選択解除でStorageと`match_photos`を削除。実環境確認済み）
- `[x]` Storageの読み取り権限確認（公開バケット・`anon`読み取り権限を確認済み）

## 7. 画面表示・集計

- `[x]` 総対局数カードの詳細表示（対象期間の局数・半荘／東風内訳）
- `[x]` 対局一覧10件表示＋スクロール
- `[x]` 対局修正ボタン
- `[x]` 前戦詳細モーダル（同一日の複数対局は登録時刻を考慮）
- `[x]` 各日程の可／未定／不可詳細
- `[x]` 個人成績・直近10局平均順位
- `[x]` 累計ポイント推移の期間開始0pt表示
- `[x]` 月間・週間・年間切替
- `[x]` 同一日の複数対局を正しく表示（`game_id` 単位で集計）

## 8. 既存データ移行

- `[x]` Google Sheetsの列定義を確定（`docs/SUPABASE_MIGRATION_MAPPING.md`）
- `[x]` メンバー・対局本体・対局参加者・スケジュール・ルール移行（実投入・確認済み：11／13／52／148／2件）
- `[x]` ルール設定移行（半荘・東風の2設定）
- `[x]` 写真リンク／ファイル移行（GASの8行・ユニーク2ファイルをStorageへ移行、メタ情報2件を保存）
- `[x]` 移行前後の件数照合（11／13／52／148／2／13件）
- `[x]` 移行前後のポイント照合（既存GAS結果とSupabase `match_players` を照合済み）
- `[?]` 本番切替時点と旧GASの停止タイミング

## 9. セキュリティ・運用

- `[x]` 認証方式を決定（利用者ログインなし。URLを知る利用者が閲覧・登録可能）
- `[x]` 管理者と一般利用者の権限を決定（厳密な区別なし。会社内娯楽用途）
- `[x]` 書き込みをEdge Function経由に限定（DB直接書き込みは許可しない）
- `[x]` `service_role` キーをフロントへ出さない確認
- `[x]` 監査ログの閲覧者を決定（公開せずサーバー側のみ）
- `[x]` Supabaseバックアップ・復元手順を文書化（実バックアップ取得は運用開始時に実施）
- `[x]` Cloudflare環境変数の設定は不要と判断（現構成はブラウザ→Supabase直結。Cloudflare Workerを中継にする場合のみ秘密鍵が必要）
- `[x]` Cloudflare静的アセットの公開範囲を制限（`.assetsignore`でDB・Functionソース・移行資料を除外）

## 10. テスト・公開

- `[x]` 対局登録テスト（公開Edge Function、HTTP 200、`match_players`保存・ポイント計算を確認）
- `[x]` 対局修正テスト（公開Edge Functionでテスト対局を修正し、テストデータを削除済み）
- `[x]` 同一日の複数対局テスト（DB構造・`game_id`単位集計を確認。画面の本番疎通は未完了）
- `[x]` 半荘／東風計算テスト（ルール別設定・計算処理を確認）
- `[x]` ルール変更後も過去対局が変わらないテスト（`match_rule_snapshots`保存・参照を確認）
- `[x]` 写真アップロードテスト（公開Edge Functionで圧縮画像の登録・Storage保存を確認）
- `[x]` 直近10局・累計ポイント集計テスト（コード・画面ロジック確認）
- `[ ]` スマートフォン表示テスト
- `[ ]` 本番Cloudflare URLでの疎通確認（現URLは旧GAS画面を表示）
- `[x]` 旧GASとの最終件数照合（読み取り照合：members 11／対局 13／参加者 52／schedule 148／写真2。Supabase側と一致）
- `[ ]` 本番切替

## 現在の到達点

DB基盤、公開読み取り権限、既存データ移行、Forge側のSupabase読み書き実装、公開Edge Functionの登録・修正・削除・写真処理テスト、バックアップ・復元手順の文書化まで完了しています。残りはスマートフォン試験、CloudflareへのForge公開、本番切替です。公開手順は`docs/CLOUDFLARE_RELEASE.md`に固定済みです。

## 判断が必要になったら確認する項目

以下は実装中に決める必要があるため、勝手に確定しません。

1. ログインを導入するか、当面は管理用URLだけで運用するか
2. 対局登録・修正を誰まで許可するか
3. 旧GASをいつ停止するか
4. 写真を既存Google DriveからSupabase Storageへ完全移行するか
5. Supabaseの無料枠を超えた場合の運用方針
